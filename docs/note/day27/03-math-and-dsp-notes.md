# Ghi chú toán và DSP

- `T=N/Fs`.
- Nyquist trực giác: chỉ biểu diễn tần số dưới `Fs/2`.
- Matrix signal chuẩn có shape `(n_samples, n_channels)`.
- Unit conversion phải explicit.
- Group split là bài toán phân hoạch tập subject, không phải random từng row/window.
