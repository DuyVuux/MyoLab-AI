# Hướng dẫn đọc code

Đọc `contracts.py` trước, sau đó `source_record.py`, `safe_archive.py`, `profile_driven_csv_adapter.py`, `group_split.py`, cuối cùng `engineering_gate.py`. Với mỗi file, ghi Input → Validation → Output → Failure codes.
