# Leakage notes

Các windows cùng repetition/session/subject có tương quan. Split trước windowing. Mọi normalization/selector/calibrator/threshold học từ dữ liệu phải nằm trong inner train/validation tương lai.
