# Câu hỏi mở Day 9

1. Noraxon export là raw hay đã lọc/rectify?
2. Hardware filter và sampling rate thực tế là bao nhiêu?
3. Nên dùng một Welch segment hay nhiều segment khi có dữ liệu thật?
4. Dải 20–400 Hz có phù hợp mọi muscle/protocol không?
5. Có cần lưu toàn PSD hay chỉ feature MDF/MNF trong production?
6. Có cần normalize PSD theo total power ở dashboard không?
7. Review lâm sàng nào cần trước khi dùng spectral trend trong report?

Mỗi câu hỏi phải có trạng thái: `open`, `answered`, hoặc `external_review_required`.
