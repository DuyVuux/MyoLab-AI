# Todo Day 31

- tạo authorization riêng cho model fitting;
- tạo dependency lock thật;
- chọn smoke/full cohort;
- train Mendeley và GRABMyo độc lập;
- dùng grouped validation;
- chạy dummy, LDA, logistic, linear SVM, RF;
- không mở sealed final test;
- registry state chỉ `research`.
