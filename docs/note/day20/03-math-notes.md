# Ghi chú toán Day 20

## Data size
`Bytes = duration × Fs × channels × bytes_per_sample`

## Downsample
`stride = ceil(N / max_points)`

## RMS
`RMS = sqrt(sum(x^2)/N)`

## Usable ratio
`accepted / planned`
