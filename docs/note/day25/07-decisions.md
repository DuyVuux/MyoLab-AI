# Decisions Day 25

- `trainingAllowed=false`.
- `implementationAllowed=true` cho catalog/schema/adapter scaffolding.
- JSON là downstream project contract, không gọi native MR3.
- MFCV disabled cho site cho tới khi eligibility verified.
- Task C metric-first.
- Conflicts không được silently resolved.
- Restricted data không được tải ngoài DUA/IRB.
