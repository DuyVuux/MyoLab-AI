import test from 'node:test'; import assert from 'node:assert/strict'; import {buildReviewQueue,allowedActions,validateReviewAction,applyReviewAction} from '../src/features/review-queue/model.mjs';
const c=[{case_id:'P',attention:'PASS',review_state:'REVIEWING'},{case_id:'U',attention:'UNKNOWN',review_state:'REVIEWING'},{case_id:'F',attention:'FAIL',review_state:'REVIEWING'},{case_id:'W',attention:'WARNING',review_state:'REVIEWING'}];
test('exception-first',()=>assert.deepEqual(buildReviewQueue(c).map(x=>x.case_id),['F','W','U','P'])); test('fail cannot accept',()=>assert(!allowedActions(c[2]).includes('ACCEPT_TECHNICAL'))); test('viewer cannot mutate',()=>assert.deepEqual(allowedActions(c[0],'VIEWER'),[])); test('override reason',()=>assert.throws(()=>validateReviewAction({caseRecord:c[0],action:'OVERRIDE_WITH_REASON'}),/REASON_REQUIRED/)); test('server guard',()=>assert.throws(()=>validateReviewAction({caseRecord:{...c[0],review_state:'NEW'},action:'ACCEPT_TECHNICAL'}),/SERVER_STATE_GUARD_REJECTED/)); test('reprocess',()=>assert.equal(applyReviewAction({caseRecord:c[1],action:'REQUEST_REPROCESS',reason_code:'RAW_REVIEW'}).review_state,'REPROCESS_REQUESTED'));
