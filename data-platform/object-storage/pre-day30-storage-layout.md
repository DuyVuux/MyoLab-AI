# Pre-Day30 storage layout

Canonical absolute roots are defined in `data-platform/configs/pre_day30_storage.local.yaml`.

The repository stores only control-plane artifacts. Large data and complete EDA evidence remain under `/home/duyvd9/massive/projects/myolab-ai-data/`.

Never use `git add -f` to override raw-data ignore rules.
