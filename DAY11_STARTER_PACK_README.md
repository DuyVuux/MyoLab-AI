# Bộ khởi tạo Day 11 — Trend Features v0.1

## Điều kiện bắt đầu

Day 10 phải pass và đã commit.

```bash
unzip -l /duong-dan/day11_starter_pack.zip
unzip -n /duong-dan/day11_starter_pack.zip
bash scripts/dev/run_day11_checks.sh
```

Chạy kèm full regression Day 10:

```bash
DAY11_FULL_REGRESSION=1 bash scripts/dev/run_day11_checks.sh
```

Không bắt đầu Day 12 trước khi trend dùng đúng `center_time_s`, không ghép native grids bằng `window_index`, checker pass và Day 11 đã commit.
