# Public-format fixtures
These binary/header pairs are **synthetic format fixtures**, not downloaded public waveform
samples. They mimic only the verified WFDB grammar needed to test adapter mechanics. They
must never be counted as DAY67 public benchmark evidence or clinical/public signal truth.
