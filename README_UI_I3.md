# UI-I3 integration note
This overlay is incremental after UI-I2. It does not alter UC1–UC4/fatigue logic.
The live integration agent must bind `AutoDataEvidenceBackend` to canonical signal/processing/metric/review/audit services, reuse existing SignalViewer/MetricEvidenceCard, run a real-mode smoke, and only then accept `AUTO_DATA_EVIDENCE_READY`.
