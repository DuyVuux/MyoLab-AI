export interface MetricPoint{readonly label:string;readonly value:number;}
export interface AccessibleMetricChartProps{readonly title:string;readonly unit:string;readonly points:readonly MetricPoint[];}
export function AccessibleMetricChart({title,unit,points}:AccessibleMetricChartProps):JSX.Element{const max=Math.max(1,...points.map(p=>p.value));return <figure><figcaption>{title}</figcaption><svg role="img" aria-label={`${title}. Xem bảng dữ liệu ngay sau biểu đồ.`} viewBox="0 0 400 140">{points.map((p,i)=><rect key={p.label} x={20+i*70} y={120-(p.value/max)*100} width="40" height={(p.value/max)*100}/>)}</svg><table><caption>Dữ liệu thay thế cho {title}</caption><thead><tr><th>Mốc</th><th>Giá trị ({unit})</th></tr></thead><tbody>{points.map(p=><tr key={p.label}><td>{p.label}</td><td>{p.value}</td></tr>)}</tbody></table></figure>;}
