# DAY11 Immutable Raw & Source Ledger Handoff

Repository-ready handoff for DAY11. Apply `repo_patch/` only after collision review. No real patient data is included. Run `bash scripts/dev/run_day11_checks.sh` after integration.

The package implements source-level integrity/provenance only. It does not implement MR4 parsing, canonical Session/DomainContext, QC, DSP, SSL training, or OOD modeling.
